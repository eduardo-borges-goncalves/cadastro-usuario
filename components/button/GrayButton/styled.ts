import styled from "styled-components";
import { ButtonWrapper } from "../PrimaryBlueButton/styled";

export const Wrapper = styled(ButtonWrapper)`
  background-color: var(--primary-gray) ;
  
  :hover {
    background-color: var(--primary-gray) ;
    opacity: 0.9;
  }

`